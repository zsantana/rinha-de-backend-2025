x-service-templates:
  api: &api
    image: gabrieleiro/rinha-de-backend-2025-api:10.0
    networks:
      - rinha-de-backend-2025
      - payment-processor
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: "50MB"
    volumes:
      - /tmp:/tmp
    depends_on:
      - tracker

services:
  api-1: 
    <<: *api
    hostname: api-1
    environment:
      DEFAULT_PROCESSOR_URL: "http://payment-processor-default:8080"
      FALLBACK_PROCESSOR_URL: "http://payment-processor-fallback:8080"
      TRACKER_URI: "/tmp/tracker.sock"
      ADDRESS: "/tmp/api_1.sock"

  api-2:
    <<: *api
    hostname: api-2
    environment:
      DEFAULT_PROCESSOR_URL: "http://payment-processor-default:8080"
      FALLBACK_PROCESSOR_URL: "http://payment-processor-fallback:8080"
      TRACKER_URI: "/tmp/tracker.sock"
      ADDRESS: "/tmp/api_2.sock"

  load-balancer:
    image: gabrieleiro/rinha-de-backend-2025-load-balancer:haproxy-2.0
    networks:
      - rinha-de-backend-2025
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "50MB"
    ports:
      - "9999:9999"
    volumes:
      - /tmp:/tmp
    depends_on:
      - api-1
      - api-2

  tracker:
    hostname: tracker
    image: gabrieleiro/rinha-de-backend-2025-tracker:8.0
    networks:
      - rinha-de-backend-2025
    deploy:
      resources:
        limits:
          cpus: "0.1"
          memory: "50MB"
    volumes:
      - /tmp:/tmp
    environment:
      ADDRESS: "/tmp/tracker.sock"

networks:
  rinha-de-backend-2025:
    name: rinha-de-backend-2025
    driver: bridge
  payment-processor:
    external: true
