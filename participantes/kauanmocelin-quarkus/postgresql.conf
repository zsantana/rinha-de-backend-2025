max_connections = 20                # Poucas conexões simultâneas, para não estourar memória
shared_buffers = 32MB              # Cerca de 20% da RAM para cache interno
effective_cache_size = 48MB        # Estimativa do cache do SO (menos que a RAM total)
maintenance_work_mem = 8MB         # Operações de manutenção com memória reduzida
checkpoint_completion_target = 0.9
wal_buffers = 4MB                  # Pequeno buffer WAL para não gastar memória demais
default_statistics_target = 100
random_page_cost = 1.1
effective_io_concurrency = 2       # Baixo, pois provavelmente não tem disco rápido
work_mem = 512kB                   # Memória para ordenação e hash, reduzido para poupar memória
huge_pages = off                   # Provavelmente não vale a pena para servidores pequenos
min_wal_size = 128MB
max_wal_size = 512MB
listen_addresses = '*'
