worker_processes auto;
worker_rlimit_nofile 65535;

events {
    use epoll;
    worker_connections 4096;
    multi_accept on;
}

http {
    access_log off;
    error_log /usr/local/openresty/nginx/logs/error.log error;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    keepalive_requests 1000;

    client_body_buffer_size 16k;
    client_max_body_size 1m;

    upstream backend {
        server unix:/var/run/sockets/app1.sock max_fails=2 fail_timeout=30s;
        server unix:/var/run/sockets/app2.sock max_fails=2 fail_timeout=30s;
        keepalive 400;
        keepalive_requests 1000;
        keepalive_timeout 60s;
    }

    server {
        listen 9999 reuseport;

        location /health {
            return 200 '{"status":"healthy","nginx":"ok"}';
            add_header Content-Type application/json;
        }

        location / {
            proxy_pass http://backend;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Request-ID $request_id;

            proxy_connect_timeout 1s;
            proxy_send_timeout 1s;
            proxy_read_timeout 1s;

            proxy_buffering off;
            proxy_request_buffering off;
        }
    }
}