services:
  # -----
  # INFRA
  # -----
  cache:
    image: redis:7.2-alpine
    hostname: cache
    command: redis-server --save "" --appendonly no --maxclients 20000 --maxmemory 40mb --maxmemory-policy noeviction --tcp-keepalive 60
    networks:
      - backend
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: "0.1"
          memory: "40MB"

  # -------------
  # LOAD BALANCER
  # -------------
  nginx:
    image: nginx:1.25-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      api1:
        condition: service_started
      api2:
        condition: service_started
    ports:
      - "9999:9999"
    networks:
      - backend
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.1"
          memory: "35MB"

  # ---
  # API
  # ---
  api1: &api
    image: luzin7/rinha-2025-app:latest
    pull_policy: always
    hostname: api1
    command: node --max-old-space-size=50 --gc-interval=100 dist/app.js
    environment:
      - APP_PORT=9999
      - PAYMENT_PROCESSOR_URL_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_PROCESSOR_URL_FALLBACK=http://payment-processor-fallback:8080
      - CACHE_HOST=cache
      - CACHE_PORT=6379
      - NODE_ENV=production
    networks:
      - backend
      - payment-processor
    depends_on:
      cache: 
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.15"
          memory: "65MB"

  api2:
    <<: *api
    hostname: api2

  # ------
  # WORKER
  # ------
  worker1:
    image: luzin7/rinha-2025-app:latest
    pull_policy: always
    hostname: worker1
    command: node --max-old-space-size=120 --gc-interval=100 dist/bootstrap.js
    environment:
      - APP_PORT=9999
      - PAYMENT_PROCESSOR_URL_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_PROCESSOR_URL_FALLBACK=http://payment-processor-fallback:8080
      - CACHE_HOST=cache
      - CACHE_PORT=6379
      - NODE_ENV=production
    networks:
      - backend
      - payment-processor
    depends_on:
      cache: 
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: "145MB"

networks:
  backend:
    driver: bridge
  payment-processor:
    name: payment-processor
    external: true
