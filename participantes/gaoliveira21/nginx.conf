worker_processes auto;

events {
	worker_connections 256;
}

http {
	access_log off;

	upstream api {
		server app01:8080;
		server app02:8080;
	}

	server {
		listen 9999;

		location / {
			proxy_pass http://api;
		}
	}
}